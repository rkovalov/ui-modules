{
  "name": "ui-modules",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "clean": "pnpm run clean:workspaces && rm -rf node_modules",
    "clean:workspaces": "turbo clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "prerelease": "turbo run build --filter=docs^...",
    "release": "changeset publish",
    "format": "turbo format",
    "format:fix": "turbo format -- --write",
    "lint": "turbo lint",
    "lint:fix": "turbo lint -- --write",
    "format:lint": "turbo format:lint",
    "format:lint:fix": "turbo format:lint -- --write",
    "typecheck": "turbo typecheck",
    "type-coverage": "type-coverage --detail",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.2",
    "@changesets/cli": "^2.27.8",
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "turbo": "^2.1.2",
    "type-coverage": "^2.29.1",
    "typescript": "^5.6.2"
  },
  "lint-staged": {
    "*.{js,ts,tsx,css,md,json,css}": ["biome check --write --no-errors-on-unmatched"]
  },
  "packageManager": "pnpm@9.9.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
