{
  "name": "ui-modules",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "clean": "pnpm run clean:workspaces && rm -rf node_modules",
    "clean:workspaces": "turbo clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "prerelease": "turbo run build --filter=docs^...",
    "release": "changeset publish",
    "format": "turbo format",
    "format:fix": "turbo format -- --write",
    "lint": "turbo lint",
    "lint:fix": "turbo lint -- --write",
    "lint:format": "turbo format:lint",
    "lint:format:fix": "turbo format:lint -- --write",
    "format:css": "turbo format:css",
    "format:css:fix": "turbo format:css -- --write",
    "typecheck": "turbo typecheck",
    "type-coverage": "type-coverage --detail",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "type-coverage": "^2.29.1",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.9",
    "@commitlint/config-conventional": "^19.2.2",
    "@commitlint/cli": "^19.4.0",
    "@changesets/cli": "^2.27.7",
    "turbo": "^2.0.12",
    "typescript": "^5.5.4"
  },
  "lint-staged": {
    "*.{js,ts,tsx,css,md}": ["biome check --write --no-errors-on-unmatched"],
    "*.json": ["biome check --write --no-errors-on-unmatched"],
    "*.css": ["prettier --check --write"]
  },
  "packageManager": "pnpm@9.9.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
